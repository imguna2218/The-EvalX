# Bind to all interfaces (for Docker accessibility)
bind 0.0.0.0

# Set a max memory limit (e.g., 256MB)
maxmemory 256mb

# Eviction policy when memory is full
maxmemory-policy allkeys-lru

# Enable AOF persistence (optional, for durability)
appendonly yes

# Port to listen on
port 6379